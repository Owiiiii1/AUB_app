# Next steps

Do **not** start these in the same task as the auth foundation.

1. Create test actor accounts on the backend (student, parent, teacher) and run a real end-to-end Flutter login on a device/emulator.
2. First product slice: Student/Parent Home + schedule (read-only), still against the Laravel API contract.
3. Later: attendance, teacher flows, documents, communications — each as its own API + Flutter slice.

Still out of scope until explicitly requested: registration, password reset, biometrics, refresh tokens, push, certificate pinning, Flutter Web.
