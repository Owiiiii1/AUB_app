# Next steps

1. PM visual pass on Student / Parent / Teacher screens vs `AUB_admin/docs/ref/{student,parent,teacher}/*/screen.png`.
2. Later product slice: **Teacher Daily Check-in**.

Student visual source of truth: `AUB_admin/docs/ref/student/*`  
Parent visual source of truth: `AUB_admin/docs/ref/parent/*`  
Teacher visual source of truth: `AUB_admin/docs/ref/teacher/*`

Still out of scope until explicitly requested: registration, forgot/reset password, biometrics, refresh tokens, push delivery (FCM), certificate pinning, Flutter Web, unified family calendar, teacher timetable editing, attendance percentages, profile editing backend, payments, documents, chat, report cards, geolocation, new attendance statuses.
