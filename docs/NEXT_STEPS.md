# Next steps

1. Real device walkthrough of Teacher Presenze with `teacher@admin.com` (isolated week `2026-12-28`, lesson `LEZIONE CLASSICO`).
2. Later product slice: **Student/Parent Attendance History** (read-only on `attendance_records`).

Still out of scope until explicitly requested: registration, password reset, biometrics, refresh tokens, push, certificate pinning, Flutter Web, unified family calendar, teacher timetable editing, check-in.
