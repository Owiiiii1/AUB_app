# Next steps

1. Real device/emulator login with `student@admin.com` / `parent@admin.com` and walk Orario (current / prev / next).
2. Later product slices: attendance, teacher schedule, documents, communications — each with its own API + Flutter task.

Still out of scope until explicitly requested: registration, password reset, biometrics, refresh tokens, push, certificate pinning, Flutter Web, unified family calendar.
