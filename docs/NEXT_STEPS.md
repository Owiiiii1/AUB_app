# Next steps

1. PM visual pass on Parent screens vs `AUB_admin/docs/ref/parent/*/screen.png`.
2. Next production stage: **Teacher mobile UI** (`docs/ref/teacher`).
3. Later product slice: **Teacher Daily Check-in**.

Student visual source of truth: `AUB_admin/docs/ref/student/*` (production-ready).  
Parent visual source of truth: `AUB_admin/docs/ref/parent/*`.  
Teacher is pending.

Still out of scope until explicitly requested: registration, forgot/reset password, biometrics, refresh tokens, push delivery (FCM), certificate pinning, Flutter Web, unified family calendar, teacher timetable editing, attendance percentages, profile editing backend, payments, documents, chat, report cards.
