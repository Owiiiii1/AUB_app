# Next steps

1. Real device walkthrough of Teacher Orario with `teacher@admin.com` (current / prev / next; isolated week `2026-12-28` if the current week is unpublished).
2. Later product slices: attendance, check-in, documents, communications — each with its own API + Flutter task.

Still out of scope until explicitly requested: registration, password reset, biometrics, refresh tokens, push, certificate pinning, Flutter Web, unified family calendar, teacher timetable editing.
